import type { Lab, LabStatus } from "@ammarlab/shared";

export function MyLabsPage({ labs, statuses, onStart, onStop, onReset, onOpenRunner }: { labs: Lab[]; statuses: LabStatus[]; onStart: (id: string)=>void; onStop: (id: string)=>void; onReset: (id: string)=>void; onOpenRunner:(id:string)=>void; }) { return <section className="glass-panel page"><h3>My Labs</h3><div className="labs-grid">{labs.map((lab)=>{const st=statuses.find((s)=>s.labId===lab.id)?.state??"idle";return <article className="lab-card" key={lab.id}><h4>{lab.title}</h4><div className="lab-meta"><span className="badge">{lab.difficulty}</span><span className={`badge status-${st}`}>{st}</span></div><div className="actions"><button onClick={()=>onStart(lab.id)}>Start</button><button className="ghost" onClick={()=>onStop(lab.id)}>Stop</button><button className="ghost" onClick={()=>onReset(lab.id)}>Reset</button><button className="ghost" onClick={()=>onOpenRunner(lab.id)}>Open Runner</button></div></article>;})}</div></section>; }
