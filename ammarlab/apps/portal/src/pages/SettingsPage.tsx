import type { AppSettings } from "../services/settingsStore";

export function SettingsPage({ draftSettings, setDraftSettings, onSave, onReset }: { draftSettings: AppSettings; setDraftSettings: (s: AppSettings)=>void; onSave: ()=>void; onReset: ()=>void; }) { return <section className="glass-panel page"><h3>Settings</h3><div className="settings-grid"><label>Agent URL<input value={draftSettings.agentUrl} onChange={(e) => setDraftSettings({ ...draftSettings, agentUrl: e.target.value })} /></label><label>Local token<input value={draftSettings.localToken} onChange={(e) => setDraftSettings({ ...draftSettings, localToken: e.target.value })} /></label><label>Default provider<select value={draftSettings.defaultProvider} onChange={(e)=>setDraftSettings({...draftSettings, defaultProvider:e.target.value as AppSettings['defaultProvider']})}><option value="auto">Auto</option><option value="hyperv">Hyper-V</option><option value="vmware">VMware</option></select></label><label>UI density<select value={draftSettings.uiDensity} onChange={(e)=>setDraftSettings({...draftSettings, uiDensity:e.target.value as AppSettings['uiDensity']})}><option value="comfortable">Comfortable</option><option value="compact">Compact</option></select></label></div><div className="actions"><button onClick={onSave}>Save Settings</button><button className="ghost" onClick={onReset}>Reset Settings</button></div></section>; }
